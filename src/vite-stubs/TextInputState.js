/**
 * Stub for react-native/Libraries/Components/TextInput/TextInputState.
 * Used by Vite web build only; callers (e.g. @stripe/stripe-react-native) check
 * for method presence, so an empty object is sufficient.
 */
export default {};
